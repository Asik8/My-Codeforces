#include <stdio.h>
#include<string.h>
int main()
{
    int a,b,cont=0;
    scanf("%d %d",&a,&b);

    while(1)
    {
        if(a<b && (a-1)*2==b)
        {
            
        }
    }

    printf("%d\n",cont);

    return 0;

}