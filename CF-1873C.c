#include <stdio.h>

int main()
{
    int t;
    scanf("%d ", &t);


    while (t--)
    {
        char a;
        int sum =0;
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                scanf(" %c",&a);
                if (a == 'X')
                {
                    if ((i==0 && j>=0 && j<=9) || (j==0 && i>=0 && i<=9) || ((i==9 && j>=0 && j<=9)) || (j==9 && i>=0 && i<=9))
                    {
                        sum++;
                    }
                    else if((i==1 && j>=1 && j<=8) || (j==1 && i>=1 && i<=8) || ((i==8 && j>=1 && j<=8)) || (j==8 && i>=1 && i<=8))
                    {
                        sum += 2;
                    }
                    else if((i==2 && j>=2 && j<=7) || (j==2 && i>=2 && i<=7) || ((i==7 && j>=2 && j<=7)) || (j==7 && i>=2 && i<=7))
                    {
                        sum += 3;
                    }
                    else if((i==3 && j>=3 && j<=6) || (j==3 && i>=3 && i<=6) || ((i==6 && j>=3 && j<=6)) || (j==6 && i>=3 && i<=6))
                    {
                        sum += 4;
                    }
                    else if((i==4 && j>=4 && j<=5) || (j==4 && i>=4 && i<=5) || ((i==5 && j>=4 && j<=5)) || (j==5 && i>=4 && i<=5))
                    {
                        sum += 5;
                    }
                }
            }
        }
        printf("%d\n",sum);
    }
}